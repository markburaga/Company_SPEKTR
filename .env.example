# Telegram bot for contact-form submissions (server-side only).
# Copy to .env.local and fill in. Never commit real values.
#
# 1. Create a bot via @BotFather → get the token.
# 2. Get your chat id (e.g. via @userinfobot) or a group chat id.
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
